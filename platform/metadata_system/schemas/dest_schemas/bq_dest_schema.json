{

    "type": "object",

    "required": ["dest_type", "schema", "table"],

    "properties": {

        "dest_type": {

            "type": "string",
            "const": "BQ"
        },

        "schema": {

            "type": "string"
        },

        "table": {

            "type": "string",
            "pattern": "^(raw_)[a-z0-9_]+$",
            "minLength": 1,
            "maxLength": 30
        }
    },

    "additionalProperties": false
}